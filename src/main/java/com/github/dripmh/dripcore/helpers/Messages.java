package com.github.dripmh.dripcore.helpers;

import com.github.cyberryan1.cybercore.utils.CoreUtils;

public class Messages {

    public static String PLAYER_REQUIRED = CoreUtils.getColored( "&7This command can only be executed by a player!" );
    public static String CONSOLE_REQUIRED = CoreUtils.getColored( "&7This command can only be executed by console!" );

    public static String PERMS_DENIED = CoreUtils.getColored( "&cInsufficient permissions!" );
}